The generator matrix, uncoded data and encoded data is extracted using traditional polar encoder in this framework.
The polar framework has been extensively tested, so the test cases are pre-confirmed.